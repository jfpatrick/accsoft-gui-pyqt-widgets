============
Introduction
============

A widget that displays status via color in a form of a color circle.

It can either take a custom color, or one of the predefined statuses that map to the concrete
color (e.g. error = red).
